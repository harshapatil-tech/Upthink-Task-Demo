function myFunction() {
  // Simple log test
  Logger.log("Hello from GitHub + Apps Script!");
}

function addNumbers(a, b) {
  return a + b;
}

function testAdd() {
  const result = addNumbers(5, 10);
  Logger.log("Result = " + result);
}
